.players
  %h2 
    Player List
  - if @players.count > 0
    // - @players.each do |player|
    .list-group-item
      // = render partial: 'shared/list_options/horizontal_style_entry_all', locals: { the_list: @players }
      = render partial: 'shared/list_options/horizontal_style_entry_all', locals: { the_list: @players }

  - else
    %p
      No players signed yet...
      