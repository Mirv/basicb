%h1 Listing Dashboards

%h3 My dash
= @my_dash.first
%ul
 %li Campaigns I run
 = render partial: 'dash_campaigns'
 %li Games I am playing
 = render partial: 'dash_players'

%h3 All dashes

= render partial: 'list_dashes', locals: { list: @dashes_list }

%br

= link_to 'New Dash', new_dash_path
