= provide(:title, "Editing")
%h1 Editing neighborhood

= render 'form'

= link_to 'Show', @neighborhood
\|
= link_to 'Back', neighborhoods_path
%h1 Editing neighborhood

= render 'form'

= link_to 'Show', @neighborhood
\|
= link_to 'Back', neighborhoods_path
